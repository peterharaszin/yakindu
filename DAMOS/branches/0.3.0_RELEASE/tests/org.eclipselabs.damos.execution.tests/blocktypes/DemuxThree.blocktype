package damos.tests

blockType DemuxThree {

	input x
	output a
	output b
	output c

	function main(x) -> a, b, c {
		check(real[3]) -> real, real, real

		static assert x is real()[] :
			error "X must be real array"

		eq a = x[0]
		eq b = x[1]
		eq c = x[2]
	}

}
