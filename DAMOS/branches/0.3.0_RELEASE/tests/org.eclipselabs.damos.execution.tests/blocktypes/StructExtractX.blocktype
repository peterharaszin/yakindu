package damos.tests

blockType StructExtractX {

	input x
	output y
	
	struct MyNamedStruct {
		int x;
		int y;
	}

	function main(x) -> y {
		check(struct { int x; int y }) -> int

		static assert x is struct { int x; int y } :
			error "X must be struct { int x; int y }"

		eq y = x.x
	}

}
