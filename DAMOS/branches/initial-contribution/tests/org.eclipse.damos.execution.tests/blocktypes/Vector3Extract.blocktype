package damos.tests

blockType Vector3Extract {

	input x
	output y1
	output y2
	output y3
	
	behavior {
		check(real[3]) -> real, real, real

		static assert x is real[3]:
			error "X must be real[3]"

		eq y1 = x[0]
		eq y2 = x[1]
		eq y3 = x[2]
	}

}
