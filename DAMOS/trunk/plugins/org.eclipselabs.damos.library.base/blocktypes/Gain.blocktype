package damos.blocks

blockType Gain {

	input u
	output y
	
	parameter gain = 1

	behavior {
		check(real) -> real
		
		static assert u is real(?) :
			error "Input value must be numeric"
	
		eq y = gain * u
	}
	
}
