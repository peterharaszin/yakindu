package damos.library.base.math

blockType Round {

	input u
	output y
	
	parameter kind = 0 predefined {
		0 alias "round"
	}

	behavior {
		check(real, <0>) -> int
		
		static assert u is real(?) :
			error "Input value must be numeric"
	
		eq y = round(u)
	}
	
}
