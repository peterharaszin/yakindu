blockType damos.library.base.math.Sum

input _input[2..*] {
	parameter sign = 1
}
output y

behavior {

	func main<sign>(_input) -> y {
		check<{1, -1}>(real[2]) -> real
		
		static assert _input is real()[] :
			error "Input values must be numeric"
	
		eq y = sign * _input
	}
	
}
